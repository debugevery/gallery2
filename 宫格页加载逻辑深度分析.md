# Gallery2 宫格页加载逻辑深度分析

## 概述

本文档深度剖析 Gallery2 应用中宫格页（AlbumPage）的完整加载流程，包括线程模型、数据同步机制、OpenGL 渲染、LRU 缓存体系等核心模块。

**核心架构层次：**
1. **UI 层**：SlotView + AlbumSlotRenderer
2. **数据加载层**：AlbumSlidingWindow + AlbumDataLoader
3. **媒体数据层**：MediaSet + MediaItem
4. **缓存层**：ImageCacheService + GalleryBitmapPool + LruCache
5. **渲染层**：GLView + GLCanvas + Texture

---

## 1. 线程模型分析

### 1.1 ThreadPool 线程池配置

**文件位置**：[ThreadPool.java](file:///d:/code/gallery2/Gallery2/gallerycommon/src/com/android/gallery3d/util/ThreadPool.java)

**关键参数**：
- `CORE_POOL_SIZE = 4`：核心线程数
- `MAX_POOL_SIZE = 8`：最大线程数
- `KEEP_ALIVE_TIME = 10 秒`：空闲线程存活时间
- 使用 `LinkedBlockingQueue` 作为任务队列
- 线程优先级：`THREAD_PRIORITY_BACKGROUND`

**关键方法**：
| 方法 | 作用 |
|------|------|
| `submit(Job<T> job, FutureListener<T> listener)` | 提交任务，带监听器 |
| `submit(Job<T> job)` | 提交简单任务 |
| `Worker.run()` | 工作线程执行任务 |

**核心特性**：
- 支持任务取消（JobContext.isCancelled）
- 支持资源限制（CPU 资源计数器=2，网络资源计数器=2）
- 使用 PriorityThreadFactory 创建优先级线程

### 1.2 线程协作模型

```
主线程 (GL 线程)
    └─ AlbumDataLoader.ReloadTask ──→ 后台线程 (ReloadTask)
    └─ AlbumSlidingWindow.ThumbnailLoader ──→ ThreadPool (线程 1-8)
    └─ MediaItem.requestImage() ──→ ThreadPool (解码线程)
    └─ 回调 Handler 消息 ──→ 主线程
```

---

## 2. 数据加载流程

### 2.1 AlbumDataLoader 数据加载器

**文件位置**：[AlbumDataLoader.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/app/AlbumDataLoader.java)

**核心参数**：
- `DATA_CACHE_SIZE = 1000`：数据缓存大小
- `MIN_LOAD_COUNT = 32`：最小加载数量
- `MAX_LOAD_COUNT = 64`：最大加载数量

**内部数据结构**：
```java
MediaItem[] mData;              // 媒体项缓存
long[] mItemVersion;            // 媒体项版本
long[] mSetVersion;             // 媒体集版本
```

**关键类**：
1. `ReloadTask`：后台重新加载线程
2. `GetUpdateInfo`：获取更新信息（Callable）
3. `UpdateContent`：更新内容（Callable）

**关键方法**：
| 方法 | 作用 |
|------|------|
| `resume()` | 启动数据加载 |
| `pause()` | 暂停数据加载 |
| `setActiveWindow(start, end)` | 设置可见窗口 |
| `get(index)` | 获取指定索引的媒体项 |
| `reload()` | 重新加载数据 |

**数据加载时序**：
```
AlbumDataLoader.resume()
    ↓
ReloadTask.start()
    ↓
while (mActive)
    ├─ mSource.reload()  // 重新加载源数据
    ├─ executeAndWait(GetUpdateInfo)  // 同步获取更新信息
    ├─ info.items = mSource.getMediaItem(start, count)  // 加载数据
    └─ executeAndWait(UpdateContent)  // 更新内容到主线程
```

### 2.2 媒体集同步机制

**文件位置**：[MediaSet.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/MediaSet.java) 和 [LocalAlbum.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/LocalAlbum.java)

**ContentListener 通知机制**：
- 使用 `WeakHashMap<ContentListener, Object>` 维护监听器
- 当内容变化时，调用 `notifyContentChanged()`
- 使用 `ChangeNotifier` 监听 ContentResolver 变化

**LocalAlbum 数据查询**：
```java
getMediaItem(int start, int count)
    ↓
query(Uri, projection, where, args, order)
    ↓
cursor.moveToNext()
    ↓
创建/更新 LocalImage 对象
```

**ContentObserver 触发流程**：
```
MediaStore 内容变化
    ↓
ChangeNotifier 触发
    ↓
MediaSet.notifyContentChanged()
    ↓
AlbumDataLoader.ReloadTask 被唤醒
    ↓
重新加载数据
```

### 2.3 AlbumSlidingWindow 滑动窗口

**文件位置**：[AlbumSlidingWindow.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/AlbumSlidingWindow.java)

**核心参数**：
- `CACHE_SIZE = 96`：缓存大小（宫格页）
- `JOB_LIMIT = 2`：并发缩略图加载限制

**内部数据结构**：
```java
AlbumEntry[] mData;  // 条目数组
int mContentStart;   // 内容缓存起始索引
int mContentEnd;     // 内容缓存结束索引
int mActiveStart;    // 可见区域起始索引
int mActiveEnd;      // 可见区域结束索引
```

**AlbumEntry 数据结构**：
```java
public static class AlbumEntry {
    MediaItem item;           // 媒体项
    Path path;                // 路径
    boolean isPanorama;       // 是否全景
    int rotation;             // 旋转角度
    int mediaType;            // 媒体类型
    boolean isWaitDisplayed;  // 是否显示占位符
    TiledTexture bitmapTexture;  // 瓦片纹理
    Texture content;          // 内容纹理
    BitmapLoader contentLoader;  // 加载器
    PanoSupportListener mPanoSupportListener;  // 全景监听
}
```

**核心方法**：
| 方法 | 作用 |
|------|------|
| `setActiveWindow(start, end)` | 设置可见窗口 |
| `get(index)` | 获取条目 |
| `setContentWindow(start, end)` | 设置内容缓存窗口 |
| `prepareSlotContent(index)` | 准备槽位内容 |
| `freeSlotContent(index)` | 释放槽位内容 |
| `updateAllImageRequests()` | 更新所有图像请求 |
| `requestSlotImage(index)` | 请求槽位图像 |

**窗口管理策略**：
- 可见区域：`mActiveStart` ~ `mActiveEnd`
- 内容缓存：`mContentStart` ~ `mContentEnd`
- 当可见区域变化时，自动扩展/收缩内容缓存

**ThumbnailLoader 缩略图加载**：
```java
private class ThumbnailLoader extends BitmapLoader {
    int mSlotIndex;          // 槽位索引
    MediaItem mItem;         // 媒体项
    
    submitBitmapTask()
        ↓
    mItem.requestImage(TYPE_MICROTHUMBNAIL)
        ↓
    提交到 ThreadPool (JobLimiter 限制并发为 2)
        ↓
    后台解码
        ↓
    onLoadComplete()
        ↓
    Handler 发送 MSG_UPDATE_ENTRY
        ↓
    updateEntry()  // 在 GL 线程执行
}
```

---

## 3. 图像解码流程

### 3.1 DecodeUtils 解码工具

**文件位置**：[DecodeUtils.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/DecodeUtils.java)

**核心解码方法**：
```java
decodeThumbnail(jc, fd, options, targetSize, type)
    ↓
1. 先解码尺寸 (inJustDecodeBounds = true)
    ↓
2. 计算 inSampleSize
    ├─ MICROTHUMBNAIL：较短边 >= targetSize，中心裁剪
    └─ THUMBNAIL：较长边 >= targetSize
    ↓
3. 如果尺寸过大，限制最大像素数 (MAX_PIXEL_COUNT = 640000)
    ↓
4. 实际解码 (inJustDecodeBounds = false)
    ↓
5. 必要时 resize (scale <= 0.5)
    ↓
6. ensureGLCompatibleBitmap()  // 确保是 ARGB_8888
```

**inBitmap 复用**：
- 使用 `GalleryBitmapPool` 获取可复用的 Bitmap
- 减少 GC 压力

### 3.2 BitmapLoader 位图加载基类

**文件位置**：[BitmapLoader.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/BitmapLoader.java)

**状态机**：
```
STATE_INIT → STATE_REQUESTED → STATE_LOADED → STATE_RECYCLED
            ↓ (cancel)
         STATE_CANCELLED
```

---

## 4. LRU 缓存体系

### 4.1 LruCache 实现

**文件位置**：[LruCache.java](file:///d:/code/gallery2/Gallery2/gallerycommon/src/com/android/gallery3d/common/LruCache.java)

**核心特性**：
- 使用 `LinkedHashMap` 实现 LRU 策略
- 使用 `WeakReference` 保留弱引用
- 当条目被移除时，若还有外部引用，仍可通过 `mWeakMap` 访问

**关键方法**：
```java
put(K key, V value)
    ↓
mLruMap.put(key, value)  // 强引用缓存
mWeakMap.put(key, new Entry(...))  // 弱引用缓存
```

```java
get(K key)
    ↓
mLruMap.get(key)  // 优先从强引用取
    ↓ (没找到)
mWeakMap.get(key).get()  // 从弱引用取
```

### 4.2 GalleryBitmapPool 位图池

**文件位置**：[GalleryBitmapPool.java](file:///d:/code/gallery2/Gallery2/src/com/android/photos/data/GalleryBitmapPool.java)

**核心参数**：
- `CAPACITY_BYTES = 20971520`（20MB）
- 分3个池：
  1. 正方形位图池
  2. 常见照片比例池（4:3, 3:2, 16:9）
  3. 其他比例池

**关键方法**：
| 方法 | 作用 |
|------|------|
| `get(width, height)` | 获取可复用位图 |
| `put(bitmap)` | 放入位图到池 |
| `clear()` | 清空池 |

### 4.3 ImageCacheService 图像缓存服务

**文件位置**：[ImageCacheService.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/ImageCacheService.java)

**核心参数**：
- `IMAGE_CACHE_FILE = "imgcache"`：缓存文件名
- `IMAGE_CACHE_MAX_ENTRIES = 5000`：最大条目数
- `IMAGE_CACHE_MAX_BYTES = 200 * 1024 * 1024`（200MB）：最大字节数
- `IMAGE_CACHE_VERSION = 7`：缓存版本

**缓存键结构**：
```
path + "+" + timeModified + "+" + type → CRC64 → 缓存键
```

**关键方法**：
| 方法 | 作用 |
|------|------|
| `getImageData(path, timeModified, type, buffer)` | 获取缓存数据 |
| `putImageData(path, timeModified, type, value)` | 存入缓存数据 |
| `clearImageData(path, timeModified, type)` | 清除缓存 |

**缓存查找流程**：
```
MediaItem.requestThumbnail()
    ↓
ImageCacheService.getImageData()
    ├─ 命中 → 直接解码缓存数据
    └─ 未命中 → 从文件解码，并存入缓存
```

---

## 5. OpenGL 渲染流程

### 5.1 SlotView 宫格视图

**文件位置**：[SlotView.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/SlotView.java)

**渲染循环**：
```
GLRootView 渲染循环
    ↓
GLView.render(GLCanvas)
    ↓
SlotView.render(GLCanvas)
    ├─ 计算可见区域 (getVisibleStart(), getVisibleEnd())
    └─ 对每个可见槽位
        ├─ mSlotRenderer.onVisibleRangeChanged()
        ├─ mSlotRenderer.renderSlot(canvas, index, pass, w, h)
        └─ 根据返回值决定是否需要更多渲染遍
```

**可见性变化通知**：
```
用户滚动
    ↓
SlotView.mLayout.setScrollPosition()
    ↓
onScrollPositionChanged()
    ↓
mSlotRenderer.onVisibleRangeChanged(visibleStart, visibleEnd)
    ↓
AlbumSlidingWindow.setActiveWindow()
    ↓
更新图像请求
```

### 5.2 AlbumSlotRenderer 槽渲染器

**文件位置**：[AlbumSlotRenderer.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/AlbumSlotRenderer.java)

**渲染流程**：
```java
renderSlot(canvas, index, pass, width, height)
    ↓
1. 获取 AlbumEntry
    ↓
2. 检查纹理是否就绪 (checkTexture())
    ├─ 未就绪 → 显示占位符 (mWaitLoadingTexture)
    └─ 就绪 → 如果之前显示占位符，显示淡入动画 (FadeInTexture)
    ↓
3. 绘制内容 (drawContent())
    ↓
4. 如果是视频，绘制视频覆盖层 (drawVideoOverlay())
    ↓
5. 如果是全景，绘制全景图标 (drawPanoramaIcon())
    ↓
6. 绘制覆盖层 (renderOverlay())
    ├─ 按下状态
    ├─ 按下抬起动画
    ├─ 高亮状态
    └─ 选中状态
```

**纹理检查**：
```java
checkTexture(Texture texture)
    ↓
if (texture 是 TiledTexture 且 !isReady())
    return null
else
    return texture
```

### 5.3 纹理系统

#### 5.3.1 TiledTexture 瓦片纹理

**文件位置**：[TiledTexture.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/glrenderer/TiledTexture.java)

**瓦片管理**：
- 大图像被分成多个瓦片（Tile）
- 按需加载可见瓦片
- 上传队列管理（Uploader）

**核心方法**：
| 方法 | 作用 |
|------|------|
| `isReady()` | 检查纹理是否就绪 |
| `draw(canvas, x, y)` | 绘制纹理 |
| `freeResources()` | 释放资源 |
| `isContentValid()` | 检查内容是否有效 |

#### 5.3.2 BitmapTexture 位图纹理

**文件位置**：[BitmapTexture.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/glrenderer/BitmapTexture.java)

**上传流程**：
```
Bitmap 准备好
    ↓
在 GL 渲染线程
    ↓
BitmapTexture.onBind(GLCanvas)
    ↓
glGenTextures()
glBindTexture()
glTexParameteri()
glTexImage2D()  // 上传到 GPU
```

#### 5.3.3 Texture 基类

**文件位置**：[Texture.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/glrenderer/Texture.java)

**核心接口**：
- `isLoaded()`：是否已加载
- `getWidth()` / `getHeight()`：尺寸
- `onBind(GLCanvas)`：绑定回调
- `onRelease()`：释放回调

---

## 6. 完整数据流图

### 6.1 缩略图加载完整链路

```
┌─────────────────────────────────────────────────────────────────┐
│                     冷启动/用户进入相册                          │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  GalleryActivity.onCreate()                                     │
│  ├─ initializeByIntent()  // 根据 Intent 决定首页                │
│  └─ StateManager.startState(AlbumPage)                          │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  AlbumPage.onCreate()                                            │
│  ├─ initializeViews()                                            │
│  │   ├─ 创建 SlotView                                            │
│  │   ├─ 创建 AlbumSlotRenderer                                   │
│  │   ├─ 设置 SlotRenderer                                        │
│  │   └─ 设置 Listener                                            │
│  └─ initializeData()                                             │
│      ├─ 获取 MediaSet                                            │
│      └─ 创建 AlbumDataLoader                                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  AlbumPage.onResume()                                            │
│  ├─ setContentPane()                                             │
│  ├─ AlbumDataLoader.resume()                                     │
│  └─ AlbumSlotRenderer.resume()                                   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  AlbumDataLoader.resume()                                        │
│  ├─ mSource.addContentListener()  // 注册监听                    │
│  └─ ReloadTask.start()  // 启动后台加载线程                      │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ReloadTask.run()  [后台线程]                                     │
│  ├─ mSource.reload()                                             │
│  ├─ executeAndWait(GetUpdateInfo)  [切换到主线程同步]             │
│  ├─ mSource.getMediaItem(start, count)  // 加载媒体项             │
│  └─ executeAndWait(UpdateContent)  [切换到主线程同步]             │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  AlbumDataLoader.onSizeChanged()  // 更新尺寸                     │
│  └─ AlbumSlotRenderer.onSizeChanged()                            │
│      └─ SlotView.setSlotCount()                                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  SlotView 布局完成，渲染开始                                      │
│  ├─ render(GLCanvas)                                             │
│  ├─ mSlotRenderer.onVisibleRangeChanged()                        │
│  └─ AlbumSlidingWindow.setActiveWindow()                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  AlbumSlidingWindow.updateAllImageRequests()                     │
│  ├─ 对可见区域的每个槽位                                          │
│  │   └─ requestSlotImage(index)                                  │
│  │       └─ entry.contentLoader.startLoad()                      │
│  └─ 若活跃请求为0，开始请求非活跃槽位                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ThumbnailLoader.startLoad()                                     │
│  └─ ThreadPool.submit()  [进入线程池]                            │
│      └─ mItem.requestImage(TYPE_MICROTHUMBNAIL)                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  LocalImage.requestImage()  [后台解码线程]                       │
│  ├─ ImageCacheService.getImageData()  // 尝试缓存                 │
│  ├─ if (缓存命中)  // 从缓存解码                                  │
│  │   └─ DecodeUtils.decode()                                      │
│  └─ else  // 从文件解码                                           │
│      ├─ 打开 FileDescriptor                                      │
│      └─ DecodeUtils.decodeThumbnail()                            │
│          ├─ 先解码尺寸 (inJustDecodeBounds=true)                  │
│          ├─ 计算 inSampleSize                                     │
│          ├─ 实际解码 (inJustDecodeBounds=false)                   │
│          └─ ensureGLCompatibleBitmap()                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  解码完成后，存入缓存 (如果未命中)                                 │
│  └─ ImageCacheService.putImageData()                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  BitmapLoader.onLoadComplete()  [后台线程]                        │
│  └─ Handler.sendEmptyMessage(MSG_UPDATE_ENTRY)                   │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  Handler.handleMessage()  [GL 线程]                               │
│  └─ ThumbnailLoader.updateEntry()                                │
│      ├─ 创建 TiledTexture                                         │
│      ├─ mTileUploader.addTexture()  // 添加上传队列               │
│      ├─ 减少活跃请求计数                                          │
│      └─ mListener.onContentChanged()  // 通知内容变化             │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  SlotView.invalidate()                                            │
│  └─ 下次渲染循环时重新绘制                                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  AlbumSlotRenderer.renderSlot()  [GL 线程]                        │
│  ├─ 获取已加载好的 Texture                                        │
│  ├─ checkTexture()  // 检查 TiledTexture 是否就绪                 │
│  ├─ 如果需要淡入，使用 FadeInTexture                              │
│  ├─ drawContent()  // 绘制内容                                    │
│  ├─ 绘制视频/全景图标                                             │
│  └─ 绘制覆盖层 (按下/选中状态)                                    │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 冷启动关键时间线

```
0ms  ─ 用户点击图标
5ms  ─ GalleryAppImpl.onCreate()
10ms ─ GalleryActivity.onCreate()
      ├─ setContentView()
      └─ initializeByIntent()
15ms ─ AlbumPage.onCreate()
      ├─ initializeViews()
      └─ initializeData()
20ms ─ AlbumPage.onResume()
      ├─ AlbumDataLoader.resume()
      └─ AlbumSlotRenderer.resume()
25ms ─ ReloadTask.run()  // 后台开始
      └─ LocalAlbum.reload()
50ms ─ MediaSet 数据加载完成
      └─ AlbumDataLoader 更新数据
60ms ─ SlotView 首次渲染
      ├─ 可见区域计算
      └─ AlbumSlidingWindow.setActiveWindow()
65ms ─ 开始请求缩略图
      └─ ThumbnailLoader.startLoad()
80ms ─ 首个缩略图解码完成
100ms└─ 首个缩略图上传到 GPU 并显示
```

---

## 7. 性能优化点

### 7.1 内存优化

1. **滑动窗口缓存**：只缓存可见区域附近数据（96个槽）
2. **Bitmap 复用**：`GalleryBitmapPool` 复用 Bitmap，减少 GC
3. **TiledTexture**：大图像分块加载，按需上传
4. **弱引用缓存**：`LruCache` 使用 WeakReference 保留临时引用

### 7.2 线程优化

1. **ThreadPool 限制**：核心线程4个，最大8个，避免并发过多
2. **JobLimiter 限制**：缩略图加载最多并发2个
3. **后台线程优先级**：`THREAD_PRIORITY_BACKGROUND`，不影响 UI
4. **ReloadTask 独立线程**：与缩略图加载解耦

### 7.3 渲染优化

1. **OpenGL 渲染**：GPU 加速，高性能
2. **可见区域优先**：只加载/渲染可见区域
3. **淡入动画**：缩略图加载完成后淡入，体验更好
4. **占位符**：加载中显示占位符

### 7.4 IO 优化

1. **ImageCacheService**：缓存缩略图到磁盘
2. **inSampleSize**：解码时就缩小，避免加载大尺寸
3. **MAX_PIXEL_COUNT**：限制超大图像解码时的像素数

---

## 8. 关键模块文件清单

| 模块 | 文件 |
|------|------|
| 线程池 | [ThreadPool.java](file:///d:/code/gallery2/Gallery2/gallerycommon/src/com/android/gallery3d/util/ThreadPool.java) |
| 数据加载 | [AlbumDataLoader.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/app/AlbumDataLoader.java) |
| 滑动窗口 | [AlbumSlidingWindow.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/AlbumSlidingWindow.java) |
| 媒体数据 | [MediaSet.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/MediaSet.java) |
| 本地相册 | [LocalAlbum.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/LocalAlbum.java) |
| 本地图像 | [LocalImage.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/LocalImage.java) |
| 解码工具 | [DecodeUtils.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/DecodeUtils.java) |
| 位图加载 | [BitmapLoader.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/BitmapLoader.java) |
| 宫格视图 | [SlotView.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/SlotView.java) |
| 槽渲染器 | [AlbumSlotRenderer.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/ui/AlbumSlotRenderer.java) |
| 数据管理 | [DataManager.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/DataManager.java) |
| 图像缓存 | [ImageCacheService.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/data/ImageCacheService.java) |
| 位图池 | [GalleryBitmapPool.java](file:///d:/code/gallery2/Gallery2/src/com/android/photos/data/GalleryBitmapPool.java) |
| LRU缓存 | [LruCache.java](file:///d:/code/gallery2/Gallery2/gallerycommon/src/com/android/gallery3d/common/LruCache.java) |
| 瓦片纹理 | [TiledTexture.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/glrenderer/TiledTexture.java) |
| 位图纹理 | [BitmapTexture.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/glrenderer/BitmapTexture.java) |
| 相册页面 | [AlbumPage.java](file:///d:/code/gallery2/Gallery2/src/com/android/gallery3d/app/AlbumPage.java) |

---

## 9. 总结

Gallery2 宫格页加载是一个完整的异步、多线程、高性能的加载系统：

1. **线程模型**：使用 ThreadPool 管理后台任务，结合 Handler 实现线程间通信
2. **数据同步**：使用 ContentObserver 监听 MediaStore 变化，自动刷新
3. **缓存体系**：LruCache 内存缓存 + GalleryBitmapPool 位图池 + ImageCacheService 磁盘缓存
4. **渲染流程**：基于 OpenGL 的自定义视图系统，高性能渲染
5. **窗口管理**：AlbumSlidingWindow 滑动窗口，只缓存可见区域，内存高效

虽然 Gallery2 已不再维护，但其架构设计思想仍具有重要参考价值，特别是在多媒体应用、大图加载、缓存设计等方面。
